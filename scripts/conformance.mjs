// Placeholder conformance runner for the M0 skeleton. The real stack-neutral harness —
// walking `tests/conformance/**` (source.logo -> expected events/diagnostics), profile-aware
// and headless — lands in issue #6 (@testing). Until then this keeps the DoD `conformance`
// gate present and green with zero fixtures.
console.log("conformance: harness pending (issue #6) — 0 fixtures executed");
process.exit(0);

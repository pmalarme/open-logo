{
  "description": "issue #688 (terminal slice, review round 1): the ACTIVE half of `spec/interaction-events.md:43-47` -- `when`, `every`, `on_key`, and `on_click` are profile block-heads, and they are reserved WITHIN the Interaction & Events profile. Redefining any of the four with `define` raises `ol-reserved-word`, and all four are asserted in one program so a fix that reserved only the name a single fixture happened to pick cannot pass. `params.namespace` is `reserved` -- the RESERVED-WORD branch of the rule -- which is what distinguishes these four from the profile's ordinary primitives: the sibling `redefine-wait-reserved` pins `wait` at `namespace: \"primitive\"` because `wait` is a primitive name and not a reserved block-head. Before this fixture the corpus proved the primitive branch only, so the four block-heads' reservation -- a normative MUST -- rested on unit tests alone and no other implementation was bound by it (the same gap class the Sprites terminal slice #679 found for its own `ol-reserved-word` rule). This half of the pair is UNAFFECTED by maintainer ruling #833: `define` is one of the grammar's four declaration slots (`spec/grammar.md:382`), so declaring a profile block-head raises here before and after the ruling. Its twin `block-heads-free-core-only` proved the `only within` half, which the ruling RETRACTED -- see that fixture's description and issue #841. This is a check-mode fixture: it proves static legality only and does not execute.",
  "profiles": ["core-language", "interaction-events"],
  "check": true,
  "events": [],
  "diagnostics": [
    {
      "code": "ol-reserved-word",
      "source_span": {
        "document": "interaction-events/block-heads-reserved-under-profile/block-heads-reserved-under-profile",
        "start": [1, 8],
        "end": [1, 12]
      },
      "params": {
        "name": "when",
        "namespace": "reserved"
      },
      "message": "when is already a reserved, so it can't be redefined here.",
      "stage": "semantic",
      "severity": "error"
    },
    {
      "code": "ol-reserved-word",
      "source_span": {
        "document": "interaction-events/block-heads-reserved-under-profile/block-heads-reserved-under-profile",
        "start": [4, 8],
        "end": [4, 13]
      },
      "params": {
        "name": "every",
        "namespace": "reserved"
      },
      "message": "every is already a reserved, so it can't be redefined here.",
      "stage": "semantic",
      "severity": "error"
    },
    {
      "code": "ol-reserved-word",
      "source_span": {
        "document": "interaction-events/block-heads-reserved-under-profile/block-heads-reserved-under-profile",
        "start": [7, 8],
        "end": [7, 14]
      },
      "params": {
        "name": "on_key",
        "namespace": "reserved"
      },
      "message": "on_key is already a reserved, so it can't be redefined here.",
      "stage": "semantic",
      "severity": "error"
    },
    {
      "code": "ol-reserved-word",
      "source_span": {
        "document": "interaction-events/block-heads-reserved-under-profile/block-heads-reserved-under-profile",
        "start": [10, 8],
        "end": [10, 16]
      },
      "params": {
        "name": "on_click",
        "namespace": "reserved"
      },
      "message": "on_click is already a reserved, so it can't be redefined here.",
      "stage": "semantic",
      "severity": "error"
    }
  ]
}

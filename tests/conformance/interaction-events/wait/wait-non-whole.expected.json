{
  "description": "issue #680: `wait 1.5` is rejected with `ol-type` because a tick count must be a whole number (spec/interaction-events.md's `### wait <n>`; spec/error-model.md's `ol-type`). Validation reports on the argument span, the `primitive` event is NOT emitted (the pause never runs), and only the instruction event precedes the diagnostic.",
  "profiles": ["interaction-events"],
  "execute": true,
  "events": [
    {
      "seq": 0,
      "kind": "instruction",
      "source_span": {
        "document": "interaction-events/wait/wait-non-whole",
        "start": [1, 1],
        "end": [1, 9]
      },
      "payload": {
        "statement_kind": "Call"
      }
    }
  ],
  "diagnostics": [
    {
      "code": "ol-type",
      "source_span": {
        "document": "interaction-events/wait/wait-non-whole",
        "start": [1, 6],
        "end": [1, 9]
      },
      "params": {
        "expected": "whole number",
        "actual": "number",
        "value": 1.5,
        "operation": "wait"
      },
      "stage": "runtime",
      "severity": "error"
    }
  ]
}

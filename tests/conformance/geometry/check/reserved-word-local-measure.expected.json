{
  "description": "ol-reserved-word (issue #427, M4 audit): a `local measure` declaration inside a procedure body collides with the Geometry overlay primitive `measure` (spec/geometry-module.md's `## measure`), exactly as `define`/`struct` do. The checker flags the `local` name at its own span with `namespace: \"primitive\"`, gated on the active `geometry` profile.",
  "profiles": ["core-language", "geometry"],
  "check": true,
  "events": [],
  "diagnostics": [
    {
      "code": "ol-reserved-word",
      "source_span": {
        "document": "geometry/check/reserved-word-local-measure",
        "start": [2, 9],
        "end": [2, 16]
      },
      "params": { "name": "measure", "namespace": "primitive" },
      "stage": "semantic",
      "severity": "error"
    }
  ]
}

# Harness self-test: an empty program can never emit a move event, so the expected stream
# in detects-mismatch.expected.json must always disagree with what execution produces. This
# proves the harness detects and reports mismatches without turning the gate red (expect=mismatch).

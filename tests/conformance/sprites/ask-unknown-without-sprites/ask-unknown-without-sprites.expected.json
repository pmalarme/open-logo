{
  "description": "issue #675 (SP3): the reader is profile-blind, so `ask` parses into a ProfileStatement even without `sprites`; the checker then rejects it `ol-unknown-command` because its head name is not visible when `sprites` is inactive — proving the form is gated on the profile, not merely parsed.",
  "profiles": ["core-language", "turtle-rendering"],
  "check": true,
  "events": [],
  "diagnostics": [
    {
      "code": "ol-unknown-command",
      "source_span": {
        "document": "sprites/ask-unknown-without-sprites/ask-unknown-without-sprites",
        "start": [2, 1],
        "end": [2, 4]
      },
      "params": {
        "name": "ask",
        "suggestion": "abs"
      },
      "stage": "semantic",
      "severity": "error"
    }
  ]
}

{
  "description": "issue #675 (SP3): with `sprites` active, `ask`'s head name is a visible command, so a well-formed `ask` block checks clean — the other half of the slice alongside its runtime semantics (the C2 unknown-command gate is live).",
  "profiles": ["sprites", "turtle-rendering"],
  "check": true,
  "events": [],
  "diagnostics": []
}

# Negative: a destructuring `for` binder not followed by `in` raises `ol-bad-token` at parse
# stage (spec/grammar.md#ebnf-notation requires "for" binder "in" expression control-body).
for [:x] print :x

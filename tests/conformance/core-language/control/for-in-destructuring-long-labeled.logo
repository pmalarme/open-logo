# Core `for ... in` long `... end for` form with a destructuring `[ :x :y ]` binder
# (spec/grammar.md#ebnf-notation, spec/grammar.md#collections-records-and-comprehensions).
for [:x :y] in :points
  print :x
  print :y
end for

{
  "description": "End-to-end check-mode smoke fixture (issue #116): a tiny valid Core program opts into semantic checking (\"check\": true) and asserts an empty semantic-diagnostics list, proving parse -> check -> compare round-trips and that a clean pass is representable/fixturable. `:x` is assigned before it is read so the program stays clean under every rule now registered, including `ol-undefined-var` (#113).",
  "profiles": ["core-language"],
  "check": true,
  "events": [],
  "diagnostics": []
}

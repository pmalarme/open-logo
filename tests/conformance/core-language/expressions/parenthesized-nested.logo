# Nested parenthesized expressions `((1))` collapse through the parenthesized-expression
# primary with no dedicated wrapper node, per spec/grammar.md#expressions-and-calls. `arithmetic-parentheses.logo`
# already covers `(1 + 2) * 3` grouping overriding precedence; this covers nesting depth.
print ((1))
print (((2 + 3)))

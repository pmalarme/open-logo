# Parenthesized grouping overrides default precedence: `(1 + 2) * 3` forces the addition to
# happen before the multiplication, per spec/grammar.md#expressions-and-calls (parenthesized-expression is a primary).
print (1 + 2) * 3

# A complex arithmetic precedence chain mixing all four operators plus `mod`: `*`, `/`, and
# `mod` bind tighter than `+`/`-`, and every operator is left-associative, per spec/grammar.md#expressions-and-calls.
print 2 * 3 + 4 * 5 - 6 / 2 mod 4

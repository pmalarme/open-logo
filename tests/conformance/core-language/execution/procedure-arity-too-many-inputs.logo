# A parenthesized call with more arguments than a user procedure's total parameter count raises
# `ol-too-many-inputs` at the callee span (spec/error-model.md; issue #97/#111 shared param shape).
define double :a :b
  return :a + :b
end
print (double 5 6 7)

{
  "description": "`true or (1 / 0)` short-circuits (spec/execution-model.md:142): `or` never evaluates its right operand once the left is `true`, so the `ol-div-zero` that operand would raise is never reached. Result is `true`. Issue #95. Issue #815: the operand was `:missing` until the check before execution landed, at which point an unbound read became a *statically* decided fault and this correct program stopped running at all. `1 / 0` keeps the subject and strengthens it — the skipped operand is one that would genuinely raise (`ol-div-zero`) if it were evaluated, and its absence from the diagnostics is the proof.",
  "profiles": ["core-language"],
  "execute": true,
  "events": [
    {
      "seq": 0,
      "kind": "instruction",
      "source_span": {
        "document": "core-language/execution/logical-or-short-circuit",
        "start": [1, 1],
        "end": [1, 21]
      },
      "payload": {
        "statement_kind": "Call"
      }
    },
    {
      "seq": 1,
      "kind": "print",
      "source_span": {
        "document": "core-language/execution/logical-or-short-circuit",
        "start": [1, 1],
        "end": [1, 21]
      },
      "payload": {
        "values": [true]
      }
    }
  ],
  "diagnostics": []
}

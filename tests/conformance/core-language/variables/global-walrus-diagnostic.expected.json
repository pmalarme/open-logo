{
  "description": "`:=` is not an OpenLogo token and the global ruling does not add one, so global count := 0 reports ol-bad-token at the colon and again at the `=` the parser then finds where an expression was expected. The `:x := 5` regression twin this guards alongside is measured by its own case in local-variables/global-variables unit tests, not by this fixture.",
  "profiles": ["core-language"],
  "expect": "match",
  "events": [],
  "diagnostics": [
    {
      "code": "ol-bad-token",
      "source_span": {
        "document": "core-language/variables/global-walrus-diagnostic",
        "start": [1, 14],
        "end": [1, 15]
      },
      "params": { "text": ":" },
      "stage": "parse",
      "severity": "error"
    },
    {
      "code": "ol-bad-token",
      "source_span": {
        "document": "core-language/variables/global-walrus-diagnostic",
        "start": [1, 15],
        "end": [1, 16]
      },
      "params": { "text": "=" },
      "stage": "parse",
      "severity": "error"
    }
  ]
}

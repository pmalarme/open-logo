{
  "description": "Core `filter` with a destructuring `[:x :y]` item binder (spec/grammar.md:137-138) over a list of pairs parses with no diagnostics, and `check()` resolves both `:x`/`:y` reads inside the boolean comprehension body cleanly.",
  "profiles": ["core-language"],
  "check": true,
  "events": [],
  "diagnostics": []
}

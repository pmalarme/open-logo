# A bracketed LIST literal in expression position and a bracketed control BLOCK body reuse the
# same `[ ... ]` delimiters but parse to different AST shapes (ListLit vs Block), per
# spec/grammar.md#expressions-and-calls (list-literal) and the block production covering control bodies.
print [1 2 3]
repeat 3 [ print 1 ]

# A field list must contain at least one identifier (grammar.md's `field-list ::= "[" identifier
# { identifier } "]"`). Both brackets are present, so an empty `[ ]` is reported as ol-bad-token
# at the closing bracket (like an empty group `( )`), not silently accepted. Parse-only fixture.
struct point [ ]

# A wrong separator keyword — `from` where `add` requires `to` — is reported as ol-bad-token at the
# offending token, not silently accepted (spec/error-model.md). Parse-only negative fixture.
add 5 from :nums

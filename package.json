{
  "name": "openlogo",
  "version": "0.2.0",
  "private": true,
  "description": "OpenLogo — a modern, open, educational reimagining of Logo (TypeScript 7 monorepo).",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "build": "tsc -b",
    "dev": "npm run dev --workspace @openlogo/studio",
    "typecheck": "tsc -b",
    "lint": "biome lint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "pretest": "npm run -s build",
    "test": "node --test",
    "precoverage": "npm run -s build",
    "coverage": "node scripts/coverage.mjs",
    "preconformance": "npm run -s build",
    "conformance": "node scripts/conformance.mjs",
    "preexamples": "npm run -s build",
    "examples": "node scripts/check-examples.mjs && node scripts/check-markdown-examples.mjs",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "prettier": "^3.4.2",
    "typescript": "^7.0.2"
  }
}
